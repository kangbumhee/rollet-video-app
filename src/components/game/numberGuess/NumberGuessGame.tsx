"use client";

import type { GameComponentProps } from "../GameContainer";

export default function NumberGuessGame(props: GameComponentProps) {
  void props;
  return <div className="text-gray-400 text-center py-20">숫자맞추기 게임은 준비 중입니다.</div>;
}
